#!/usr/bin/env bash
set -euo pipefail
STATE="${XDG_STATE_HOME:-$HOME/.local/state}/hyprx-wallpaper"
for _ in {1..30}; do
  awww query >/dev/null 2>&1 && break
  sleep .1
done
if [[ -f "$STATE" ]] && [[ -f "$(cat "$STATE")" ]]; then
  hyprx-wallpaper "$(cat "$STATE")"
fi